# Hugging Face Space (CPU) dependencies. Kept minimal so the free CPU Space
# boots fast. No torch/transformers needed -- llama.cpp runs the GGUF directly.
gradio==5.7.1                 # chat UI
llama-cpp-python==0.3.2       # CPU GGUF inference (prebuilt CPU wheel)
huggingface_hub==1.19.0       # pull the GGUF from the hub at startup
